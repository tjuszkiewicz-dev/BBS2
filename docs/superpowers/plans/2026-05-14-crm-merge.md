# Stratton Prime CRM → BBS2 Merge — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Merge Stratton Prime CRM core features (Kalkulator Eliton Prime™ + Pipeline CRM) into BBS2 as the leading layout.

**Architecture:** Tax engine in `lib/crm/tax-engine/` (TypeScript port of Polish ZUS/PIT logic), CRM API routes under `/api/crm/`, shared UI components under `components/crm/`. Network dashboard gets sidebar layout with CRM views. Admin dashboard gets CRM tabs.

**Tech Stack:** Next.js 15 App Router, Supabase (vogyfffzlucppmddqsqw), React 19, TypeScript, Lucide icons, Baltic Teal (#4a95a9)

---

## Completed Tasks

### Task 1: Supabase CRM Migrations ✅
Applied to project `vogyfffzlucppmddqsqw`:
- `leads` — pipeline leadów z RLS per rola
- `crm_client_profiles` — profile klientów CRM
- `crm_client_activities` — historia aktywności
- `payroll_calculations` — historia kalkulacji
- `calculator_configs` — konfiguracja ZUS/PIT (z default 2026)

### Task 2: Tax Engine ✅
Files: `lib/crm/tax-engine/`
- `types.ts` — all types (Pracownik, Firma, GlobalneWyniki, etc.)
- `constants.ts` — DEFAULT_CONFIG (stawki 2026)
- `pit.ts` — `obliczPit()` z ulgami i progami
- `zus.ts` — `obliczZusPracownik/Pracodawca/Zdrowotna()`
- `grossUp.ts` — `znajdzBruttoDlaNetto()` binary search + `obliczNettoZBrutto()`
- `calculator.ts` — `obliczPracownika()` + `obliczGlobalnie()` (Standard + Split models)
- `index.ts` — barrel exports

### Task 3: CRM API Routes ✅
- `GET/POST /api/crm/leads` — lista + tworzenie leadów (duplekacja NIP)
- `GET/PUT/DELETE /api/crm/leads/[id]` — CRUD
- `POST /api/crm/leads/[id]/qualify` — kwalifikacja
- `POST /api/crm/leads/[id]/convert` — konwersja → crm_client_profiles
- `POST /api/crm/kalkulator/calculate` — obliczenie (+ opcjonalny save do DB)

### Task 4: Calculator Wizard UI ✅
Files: `components/crm/calculator/`
- `CalculatorWizard.tsx` — 6-step wizard (firma → pracownicy → standard → split → savings → summary)
- `steps/Step0Company.tsx` — dane firmy + GUS lookup
- `steps/Step1Employees.tsx` — lista pracowników z pełnymi opcjami ZUS/PIT
- `steps/Step2Standard.tsx` — model standard per pracownik (tabela + KPI)
- `steps/Step3Split.tsx` — model Eliton Prime™ per pracownik
- `steps/Step4BusinessCase.tsx` — business case, ROI, edytowalny % prowizji
- `steps/Step5Summary.tsx` — eksport CSV + zapis do DB

### Task 5: Pipeline Kanban ✅
File: `components/crm/pipeline/PipelineKanban.tsx`
- Kolumny: nowy → w trakcie → zakwalifikowany → pozyskany → odrzucony
- Fetch z `/api/crm/leads`, quick actions (kwalifikuj/pozyskaj/odrzuć)
- Modal dodawania leadów

### Task 6: Sidebar CRM Section ✅
`components/Sidebar.tsx`:
- SUPERADMIN: dodano CRM divider + Pipeline CRM + Kalkulator Prime
- DIRECTOR/MANAGER/ADVISOR: dodano CRM divider + Pipeline + Kalkulator
- CRM ikony w kolorze Baltic Teal (#4a95a9)

### Task 7: Dashboard Integration ✅
- `NetworkDashboardClient.tsx` — pełny layout z Sidebar + header + CRM views
- `DashboardAdminNew.tsx` — obsługa crm-pipeline i crm-kalkulator tabs

## Pending (Faza 2)
- [ ] Spotkania / Kalendarz
- [ ] Payroll Settlements
- [ ] Leaderboard + Gamification
- [ ] IMAP mailbox client
- [ ] Baza wiedzy AI (pgvector)
- [ ] Struktura org (D3)
- [ ] Autenti (podpisy)
